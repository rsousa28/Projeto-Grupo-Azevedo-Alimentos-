rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Enable full read and write access on all paths to ensure that logins, audit logs,
    // migrations, and daily controls work flawlessly under all conditions
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
